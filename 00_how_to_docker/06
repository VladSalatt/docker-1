# Запустить контейнер nginx в качестве фоновой задачи и назвать его "overlord".
# Он должен перезапускаться сам. Его порт 80 должен быть подключен к 5000 порту
# файла Char

	# Делаем это с помощью команды: docker run -d -p 5000:80 --name overlord --restart=always ngin
	# Где флаги:
	# -d включает фоновый режим
	# -p порты (-p <порт хоста>:<порт контейнера>
	# --name имя
	# --restart=always возможность постоянной самостоятельной перезагрузки
	 
		# Проверить вывод можно перейдя по ссылку http://<ip машины>:5000
		# Перекинет нас на страницу nginx
		
docker run -d -p 5000:80 --name overlord --restart=always nginx
