# Запустить оболочку из контейнера "alpine" и убедиться, что с ним можно взаимодействовать 
# через терминал. Также этот контейнер после выхода из него должен сам удаляться

	# Команда: docker run -it --rm alpine /bin/sh
	# Где:
	# -i оставляет STDIN открытым, помогает пользоваться alpine
	# -t запускает псевдотерминал
	# --rm автоматическое удаление после завершения
	# /bin/sh тк Alpine не содержит bash, мы используем /bin/sh
	
		# В итоге у нас запускается еще одни терминал
		# После выхода из него, стоит проверить его наличие командой
		# docker ps, там не должно быть его
		
docker run -it --rm alpine /bin/sh
