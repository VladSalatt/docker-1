# Из shell'a dabian утановить диспетчер пакетов все необходимое для
# компиляции кода на С и Пуша его на гит (установить git и gcc)

 	# Сначала запускаем debian контейнер командой "docker run -it --rm debian"
	# После запуска обновляем пакеты (как в роджере), и после занимаемся установкой
	# Вот команды: 
	# 				apt-get update
	# 				apt-get upgrade -y
	# 				apt-get install -y gcc git 
