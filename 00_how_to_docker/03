# Определите переменные, необходимые вашей виртуальной машине, Char
# в общем окружении вашего терминала, чтобы вы могли выполнить команду "docker ps" без ошибок.
	
	# Вывод команды "docker-machine env Char":
	# 	export DOCKER_TLS_VERIFY="1"
	# 	export DOCKER_HOST="tcp://192.168.99.100:2376"
	# 	export DOCKER_CERT_PATH="/Users/ebulwer/.docker/machine/machines/Char"
	# 	export DOCKER_MACHINE_NAME="Char"
	# Run this command to configure your shell:
	# eval $(docker-machine env Char)
	
	# В принципе тут и кроется сам ответ, нужно прописать команду eval $(docker-machine env Char)

		# Теперь при вызове команды "docker-machine ls" в графе ACTIVE будет стоят "*", а не "-".

eval $(docker-machine env Char)
