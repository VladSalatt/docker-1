# Создать виртуальную машину, использую драйвер ВМ и именем Char
# Флаг --driver указывает на каком провайдере (VirtualBox, AWS, etc) будет установлена машина,
# Также следующий аргументов мы подаем имя машина, в нашем случае "Char"

# Проверить результат можно с помощью команды: docker-machine ls

docker-machine create --driver virtualbox Char
